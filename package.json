{
  "name": "example-custom-agent",
  "private": true,
  "workspaces": [
    "example-aisdk-agent"
  ],
  "scripts": {
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
