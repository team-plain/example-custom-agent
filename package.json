{
  "name": "example-custom-agent",
  "private": true,
  "workspaces": [
    "packages/example-coding-agent",
    "packages/example-aisdk-agent"
  ],
  "scripts": {
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
