{
  "name": "example-custom-agent",
  "private": true,
  "workspaces": [
    "packages/example-coding-agent"
  ],
  "scripts": {
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "engines": {
    "node": "24.x"
  }
}
